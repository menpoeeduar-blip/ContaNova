kind = "web"
previewPath = "/"
title = "ContaNova ERP"
version = "1.0.0"
id = "artifacts/contanova"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25277

[services.development]
run = "pnpm --filter @workspace/contanova run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/contanova", "run", "build" ]
publicDir = "artifacts/contanova/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25277"
BASE_PATH = "/"
